import { Inter, Lusitana, SUSE } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });

export const lusitana = Lusitana({
  weight: ["400", "700"],
  subsets: ["latin"],
});

export const suse = SUSE({
  weight: ["400", "700"],
  subsets: ["latin"],
});
